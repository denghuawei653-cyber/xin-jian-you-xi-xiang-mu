# 团队协作规范

本文件是仓库的**硬性约定**，所有成员（含负责人）都适用。
新成员先读 [README.md](README.md) 完成环境初始化，再读本文件。

---

## 一、五条红线

违反这五条的改动，PR 一律打回：

1. **不提交 `.godot/`。** 那是每台机器各自的导入缓存，提交后全员天天冲突。
2. **大文件必须走 LFS。** 超过 1 MB 的二进制资源如果直接进普通 git 历史，
   仓库会永久膨胀（历史无法真正删除），最后 clone 慢到没人愿意参与。
3. **不提交任何密钥。** `export_presets.cfg` 里的签名口令、任何 API Token，
   一律从环境变量读取，不写进文件。
4. **不直接往 `main` / `develop` 提交。** 所有改动走 `feature` 分支 + PR。
5. **两个人不同时改同一个 `.tscn`。** 见第四节。

---

## 二、分支策略

| 分支 | 作用 | 谁能推 |
| --- | --- | --- |
| `main` | 随时可发布的稳定版本，每个提交都应能出包 | 只通过 PR 合并 |
| `develop` | 日常集成分支，功能在这里汇合 | 只通过 PR 合并 |
| `feat/*` `fix/*` `refactor/*` `art/*` `chore/*` | 个人工作分支 | 本人 |
| `release/x.y` | 发版冻结期的修复分支（可选） | 负责人 |

- 功能分支**生命周期尽量短**，超过 3 天没合并就要先 rebase 一次主干。
- 合并用 **Squash Merge**（一个功能一条记录），保持主干历史干净。
- 合并后删除远程分支，本地 `git branch -d` 清掉。

---

## 三、提交信息

格式：`<类型>(<影响范围>): <说明>`

合法类型：`feat` `fix` `docs` `style` `refactor` `perf` `test` `build` `ci` `chore` `revert`

规则：

- 主题行不超过 **50 个字符**，不加句号，用祈使句（「加入」而不是「加入了」）。
- 影响范围用模块名或文件域：`player` `ui` `save` `export` `audio`。
- 正文说明**为什么**，而不是重复代码改了什么。
- 一次提交只做一件事。改了玩法和美术资源，就拆成两条。

---

## 四、场景与资源冲突（Godot 团队最大的坑）

`.tscn` / `.tres` 是纯文本，理论上能合并，但实际上两个人在同一场景里各加一个节点，
合并出来的文件**经常能通过检查却打不开**。所以靠流程规避，而不是靠事后解冲突。

### 硬性做法

1. **一个场景同一时间只有一个负责人。** 开工前在群里说一句或在 Issue 里认领。
2. **拆小场景。** 不要把所有东西塞进 `main.tscn`。玩家、敌人、UI、关卡各自独立成场景，
   用实例化组合。这是根治冲突的唯一有效办法。
3. **不可合并的源文件先上锁。** PSD / Blend / FBX 这类：
   ```bash
   git lfs lock assets/hero.psd
   # 改完
   git lfs unlock assets/hero.psd
   ```
4. **靠代码而不是靠编辑器批量改动。** 需要批量改场景属性时，写一个 `@tool` 脚本或
   EditorScript 跑，把结果作为一次提交，比手工点几十个节点可靠得多。
5. **节点路径不要随意改名。** 脚本里的 `$Node/Path` 一改就断，改名请单独开一个提交，
   并且在 PR 里明确写出来。

### 已经冲突了怎么办

不要手改 `.tscn` 的冲突标记。按 [docs/scene-conflict-playbook.md](docs/scene-conflict-playbook.md)
的流程处理。

---

## 五、资源导入参数（`.import` 文件）

- `*.import` **必须提交**，否则别人拉下来贴图会按默认参数导入，画面直接不对。
- 改导入参数（过滤模式、压缩方式）会让 `.import` 变更，这是正常的，
  但要在 PR 里说明为什么改，避免有人以为是噪音。

---

## 六、代码规范

- 缩进用 **Tab**（Godot 官方风格），`.editorconfig` 已配好，编辑器一般会自动遵守。
- 命名：类名 `PascalCase`，变量与函数 `snake_case`，常量 `ALL_CAPS`。
- 每个脚本只挂一个 `class_name`，公共 API 加 `##` 文档注释。
- 提交前至少保证编辑器 Output 面板**没有新的报错和警告**。
- 不引入来路不明的插件。加进 `addons/` 的第三方插件需要单独一个 PR，
  并在 PR 描述里写清来源、许可证、改动点。

---

## 七、PR 要求

- 标题也用约定式提交格式。
- 一个 PR 只解决一件事。重构和加功能不要混在一起。
- **必须**填完 PR 模板里的自测清单。
- 有画面改动就贴截图或录屏，审核者不该为了看一眼效果去拉你的分支。
- CI 必须全绿。
- 至少一人审核通过才能合并；涉及 `project.godot`、`export_presets.cfg`、
  `addons/` 的改动需负责人审核。

> **⚠️ 注意：目前这些是「团队约定」，不是 GitHub 强制门禁。**
> 因为私有仓库的分支保护需要 GitHub Pro（免费账号用不了），所以 GitHub 不会
> 真的拦住你直接把 PR 合掉。请当作红线自觉遵守。
> 唯一**真的会拦你**的是本地钩子和 CI 红灯，见 README 第 10 节。

合并方式统一用 **Squash** 或 **Rebase**（仓库已禁用 merge commit，保证线性历史），
合并后远端分支会自动删除。

---

## 八、发布与导出

- `export_presets.cfg` 提交，但里面**不能有明文密钥**。签名口令请在
  Godot 的导出对话框里引用环境变量，或在 CI 里注入。
- 每个可发布的版本打 tag：`git tag -a v0.3.0 -m "第三个内部测试版"`，然后推送 tag。
- 出包产物（`build/`、`*.exe`、`*.apk` 等）**不入库**，放 Release 附件或内部分发渠道。

---

## 九、发现仓库被污染了怎么办

如果发现历史里已经混进了大文件或密钥：

1. **立即在群里同步**，不要一个人偷偷处理——重写历史影响所有人。
2. 大文件：用 `git lfs migrate import --include="*.psd"` 之类的命令迁移（需全员配合重新克隆）。
3. 密钥：**第一优先级是去服务商那里吊销这个密钥**，然后再清理历史。
   历史清理永远不能替代吊销。
